// Define the expected type for the recommendedBooks array
export interface Book {
  id: string
  title: string
  author: string
  avg_rating: number
  isbn: string
  isbn13: string
  num_pages: number
  image: string
}
